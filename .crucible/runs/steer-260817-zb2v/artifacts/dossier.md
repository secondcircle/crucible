# Review dossier — read me beside the steering document

⚠ 1 BLOCKING finding(s) SURVIVED the last fix round; 4 open divergence(s) survived the revision cap — sentences the cold reader can still build two ways; the cold read FAILED the standalone test — Zone A does not steer on its own. Listed below — do not approve without reading them.

## Fact audit (fresh-context verification of every claim — final verdict from fact-audit-final.md)
- **BLOCKING**: Behaviour and edge cases table, row "SDK emits tool, thinking, compaction or retry events" (tagged D3): "`toPortEvent` returns nothing; the event is logged at `debug` and dropped" asserts a specific log level (`debug`) for this event category. D3's own text only decides the four-event vocabulary and what `toPortEvent` drops — it says nothing about logging or levels, and neither D8 nor D9 decides a level scheme either. This directly contradicts Contracts' own "Log record" section a few lines away: "the field names, levels and nesting are the implementer's to choose against the DoD 6 bar." This is the same class of leakage earlier fact-audit rounds caught and fixed for the log schema generally (fact-audit-2's B1) and for the CRUCIBLE_AGENT-unrecognized log level specifically (fact-audit-2's M4, which removed a `warn` level for exactly this reason) — this one instance survived the fix-only round. A builder reading Contracts believes log levels are theirs to design; the same builder reading the behaviour table would build a specific `debug` level as if it were specified. Fix: drop "and logged at `debug`" from the row, or give the logging-of-dropped-events claim a real Decision.
- **minor**: Decision D3 is marked `⚠` but its own `*Reversal cost:*` line says `moderate`, not `expensive`. The format file defines `⚠` as marking entries "whose reversal would cost days or invalidate merged work" (i.e. the expensive tier) and the document's other two `⚠` entries (D1, D2) both correctly pair `⚠` with `*Reversal cost:* expensive`. D3's pairing is a self-contradiction within one 8-line block, though low-impact since "Where to push back" already separately flags D3 for reviewer attention via its own, independent mechanism.
- **minor**: Decisions are not sorted strictly by reversal cost despite "ranked most-irreversible first" being a stated format invariant: cheap entries (D5, D7, D8) interleave with moderate entries (D6, D9, D10) rather than all moderates preceding all cheaps. The two most expensive decisions (D1, D2) are correctly bubbled to the top, so this is a soft/partial violation, plausibly an intentional topic-grouping rather than a strict sort.
- **minor**: Contracts' preload paragraph adds "no `require` beyond ... `electron`, `events`, `timers`, `url` (plus their `node:` spellings)" — the "plus their `node:` spellings" detail is not present anywhere in evidence.md's transcription of Electron's sandbox documentation (which lists only the four bare names plus Buffer/process/setImmediate globals). Plausible but unreceipted; could not be independently verified without network access in this pass.

## Cold read (your persona, Zone A only, no repo access)
- verdict: FAILED the standalone test; open divergences remain
- **divergence (sentence → two builds → observable difference)**: VERBATIM SENTENCE: “Turn started, text delta, turn ended, error.” BUILD A: `turn ended` is a payload-free completion marker, and the pane retains accumulated deltas. BUILD B: `turn ended` carries authoritative final text, which the pane uses to replace or reconcile accumulated deltas. OBSERVABLE DIFFERENCE: Port tests and JSONL logs see different terminal payloads; when final text differs from concatenated deltas, the user sees different completed text.
- **divergence (sentence → two builds → observable difference)**: VERBATIM SENTENCE: “`agent:prompt` returns a turn id and events come back tagged with it; the pane disables send while a turn is live, and a prompt that arrives mid-turn anyway is refused as the new turn's terminal error while the live turn runs on.” BUILD A: Main returns a new id and emits `started` then `error`, treating refusal as D3's immediate failure. BUILD B: Main returns the id but emits only `error`, because D3 requires `started` only for accepted turns and D6 calls this one refused. OBSERVABLE DIFFERENCE: Handler/IPC tests, programmatic subscribers, and the log observe two events in A but one in B.
- **divergence (sentence → two builds → observable difference)**: VERBATIM SENTENCE: “It picks up `~/.pi/agent/auth.json` through the SDK's default resolution and otherwise starts from in-memory session and settings — tools off, provider and model named by Crucible rather than inferred.” BUILD A: `createSdkAdapter` hard-codes one Crucible provider/model pair. BUILD B: Crucible requires its own environment/config values and passes those explicitly. OBSERVABLE DIFFERENCE: With only documented credentials A makes a call while B fails for missing configuration; configured builds may use different providers/models with different billing, latency, token behavior, and output.
- **divergence (sentence → two builds → observable difference)**: VERBATIM SENTENCE: “A human runs `npm run prove:sdk` once, sees real model text stream, pastes the output into the evidence file; `npm run dev:sdk` shows the same pane on the real SDK.” BUILD A: `prove:sdk` is a headless fixed-prompt smoke program that streams events to stdout and exits on `ended`. BUILD B: `prove:sdk` launches Electron and requires the human to type in the pane. OBSERVABLE DIFFERENCE: The same command yields an automatically terminating terminal transcript and machine-checkable status in A, versus an interactive window requiring input and shutdown in B; the evidence artifacts differ.
- dead line: none
- **unanswerable**: The actual port data contract: event payloads and invariants, terminal-text semantics, public error shape, turn-id type/uniqueness, and structured-clone-safe values.
- **unanswerable**: Whether a refused overlapping prompt emits `started` before its terminal `error`.
- **unanswerable**: The provider/model names and the source from which Crucible obtains them.
- **unanswerable**: The `prove:sdk` prompt, execution mode, success/exit criteria, output format, and evidence-redaction rules.
- **unanswerable**: The log schema and privacy/failure policy, including redaction, console coverage, append/serialization failure, naming collisions, and retention.
- **unanswerable**: The reload/cancellation protocol: authoritative Electron lifecycle event, treatment of HMR/navigation/crash/close, guard-release timing, and fresh-session creation.
- **unanswerable**: Required behavior and debug-endpoint exposure when fixed port 9222 is occupied or externally reachable.
- **unanswerable**: The fake adapter's canned transcript, delta cadence, timer determinism, and error fixture.
- **unanswerable**: How preload invocation failure, renderer disappearance, event-send failure, and log-sink failure become visible and terminal without breaking exactly-once completion.
- **unanswerable**: SDK session inputs beyond credentials/tools/provider/model, including working directory, system prompt, and model parameters.

Full findings: fact-audit*.md, cold-read*.md. Receipts: evidence.md.
The document status is draft; approval is yours to give.