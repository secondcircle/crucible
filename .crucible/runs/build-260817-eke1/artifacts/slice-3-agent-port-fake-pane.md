# Slice 3/7: agent-port-fake-pane

**Slice sheet · generated by `build`'s staging tail from `/Users/ike/repos/crucible/.crucible/runs/build-260817-eke1/artifacts/slices.yaml`.** Master
spec: `/Users/ike/repos/crucible/.crucible/runs/steer-260817-zb2v/artifacts/steering-doc.md` — its Decisions, Contracts, edge-case table and *Not doing*
govern; where this sheet and the master disagree, the master wins. Read the
master in full before starting: this sheet is the cut, not the design.

**Goal.** Define the agent port types and build the fake adapter and chat pane against them, with no Electron involved.

## Decisions this slice realizes

Quoted from the master by their own headings; the master's prose under each
one governs, and the Contracts that name them are there too.

### D1 ⚠ The renderer reaches agents only through the agent port · aligned

### D3 ⚠ The port speaks four Crucible-owned events, each with a turn id · proposed

### D4 The chat pane always speaks over IPC; its port is handed to it · proposed

### D6 One turn at a time, correlated by turn id · proposed

### D10 Vitest + Testing Library + jsdom; unit and component tests only · aligned

**Done looks like** — these are this run's done-criteria, and nothing else is:
- `npm run dev` opens the app, a send renders the canned reply streaming in delta by delta, and send is disabled while the turn is live
- a jsdom component test mounts the pane with the fake adapter passed as a prop and asserts the full turn_started → text_delta* → turn_ended sequence rendered, importing the reply constants from the fake's module
- the port's type module is Crucible-owned TypeScript importing nothing, and no π SDK type appears in it

**Not doing:** every other slice of the ledger (`scaffold-dev-loop`, `log-sink`, `renderer-import-fence`, `agent-channel`, `logging-at-the-seam`, `sdk-adapter-and-proof`).
Slice `log-sink` landed on this branch before this run; whatever the
ledger stages after it is already staged, and is not yours to implement or
restage. Implement this slice, completely, and stop.
