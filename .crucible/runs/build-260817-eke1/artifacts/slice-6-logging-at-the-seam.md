# Slice 6/7: logging-at-the-seam

**Slice sheet · generated by `build`'s staging tail from `/Users/ike/repos/crucible/.crucible/runs/build-260817-eke1/artifacts/slices.yaml`.** Master
spec: `/Users/ike/repos/crucible/.crucible/runs/steer-260817-zb2v/artifacts/steering-doc.md` — its Decisions, Contracts, edge-case table and *Not doing*
govern; where this sheet and the master disagree, the master wins. Read the
master in full before starting: this sheet is the cut, not the design.

**Goal.** Wrap every selected adapter in `withLogging` and forward renderer console and preload errors into the same stream.

## Decisions this slice realizes

Quoted from the master by their own headings; the master's prose under each
one governs, and the Contracts that name them are there too.

### D8 Logging is a decorator at the agent-port seam · proposed

### D9 Main is the sole writer of the log stream · proposed

**Done looks like** — these are this run's done-criteria, and nothing else is:
- after a `npm run dev` session with one prompt, that launch's JSONL file shows main lifecycle, adapter identity, the prompt, every port event and a renderer console line in one order
- renderer records carry `source: "renderer"` and main records `source: "main"`, so a reader can tell the two streams apart
- a unit test drives `withLogging` over the fake adapter into the in-memory sink and asserts prompt, events and errors are recorded; no adapter references logging
- a test asserts forwarded renderer console output lands in the in-memory sink

**Not doing:** every other slice of the ledger (`scaffold-dev-loop`, `log-sink`, `agent-port-fake-pane`, `renderer-import-fence`, `agent-channel`, `sdk-adapter-and-proof`).
Slice `agent-channel` landed on this branch before this run; whatever the
ledger stages after it is already staged, and is not yours to implement or
restage. Implement this slice, completely, and stop.
